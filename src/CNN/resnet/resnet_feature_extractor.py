import os
import argparse
import chainer
from chainer import links as L
from chainer import functions as F

parser = ArgumentParser()
paraser.add_argument('--input_dir', '-id', type=str, default)
parser.add_argument('--output_dir', '-od', default)
